## Run Project
- install modules
`npm install`
- run project
`node src/app.js`


## Current Approach
By using `axios` and `Promise`, it gets datas from the endpoints and create a result json array by manipulating array.

## Other Approach


## Enhancement
- Optimze the resource usage
- Add `env` for auth and endpoints